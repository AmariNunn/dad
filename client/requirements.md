## Packages
p5 | Required for the interactive force field background
framer-motion | For smooth page transitions and entry animations
lucide-react | Modern icons for services and UI elements

## Notes
The background uses p5.js canvas which needs to be behind all content (z-index: 0).
Glassmorphism effects require backdrop-filter support.
API endpoint for quotes is POST /api/quotes.
